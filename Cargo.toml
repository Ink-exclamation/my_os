[package]
name = "my_os"
version = "0.1.0"
edition = "2024"

[dependencies]
bootloader = "0.9"
rlibc = "1.0"
spin = "0.10.0"
volatile = "0.6.1"

[dependencies.lazy_static]
version = "1.0"
features = ["spin_no_std"]
